#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: input 
 * Return: 1 if c is uppercase, 0 if otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
